import { Routes } from '@angular/router';
import { Maps } from './features/maps/maps';
import { Home } from './features/home/home';

export const routes: Routes = [
    { path: '', component: Home , title:' Mapa Mundial'},
];
